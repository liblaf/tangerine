# -*- mode: yaml; -*-

INHERIT: .config/copier/mkdocs.yaml
site_name: "Tangerine"
site_description: TODO

plugins:
  mkdocstrings:
    handlers:
      python:
        inventories:
          - https://docs.python.org/3/objects.inv
